%h1 Fight History

%table
  %thead
    %tr
      %th Timestamp
      %th Character 1
      %th Weapon 1
      %th Character 2
      %th Weapon 2
      %th Winner
  %tbody
    - @fights.reverse_each do |fight|
      %tr
        %td= fight.created_at.strftime("%d-%m-%Y %H:%M")
        %td= fight.character1.name
        %td= fight.weapon1 ? fight.weapon1.name : "No weapon"
        %td= fight.character2.name
        %td= fight.weapon2 ? fight.weapon2.name : "No weapon"
        %td= fight.winner ? fight.winner.name : "It's a tie!"


= link_to 'Back to Home', root_path
